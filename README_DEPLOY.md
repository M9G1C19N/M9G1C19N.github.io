# Portfolio Upgrade Deployment

Copy these files into your GitHub Pages repository root:

- `index.html`
- `assets/profile.png`

Then commit and push:

```bash
git add index.html assets/profile.png
git commit -m "Improve portfolio responsiveness, design, and scroll animations"
git push
```
